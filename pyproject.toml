# Not a package: these are scripts run in place, on two machines with two
# different environments (see README). This file carries the lint settings
# only, matched to the application repo's.
[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B"]
